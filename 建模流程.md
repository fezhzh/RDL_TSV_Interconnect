# v12 model （代码参考v8 model）
器件结构：TMRDL-TSV-BSMRDL-TSV-TMRDL-TSV-BSMRDL-TSV-TMRDL-TSV-BSMRDL-TSV-TMRDL 级联结构。器件参数见Appendix 4。
建模数据位置：详见Appendix 6。
建模方法：
1. 基于HFSS仿真数据，对TMRDL、BSMRDL、TSV建立等效电路模型。
2. 在每一个RDL和TSV的连接处，插入一个级联网络（pi型电路，详见Appendix 1）。
3. 使用优化算法优化pi型电路的参数，使得级联网络的S参数与ADS仿真得到的S参数尽可能接近。
4. 使用优化得到的pi型电路参数，和对应的器件参数构建数据集。用于训练神经网络，实现器件参数到pi型电路参数的映射。
5. 使用训练好的神经网络，输入器件参数，得到对应的pi型电路参数。然后利用这些参数构建级联网络，连接使用ADS仿真得到RDL和TSV的S参数，得到整体结构的S参数。

具体建模流程
1. 使用.\HFSS_sim\LHS400_Connection2\train 目录下的RDL和TSV数据，建立RDL(BSMRDL和TSMRDL使用同一个模型)和TSV的等效电路模型，使得模型的S参数与HFSS仿真得到的S参数尽可能接近。
2. 读取HFSS仿真得到的S参数，使用对应的器件参数，使用1中模型计算得到RDL和TSV的S参数
3. 级联ADS仿真得到的RDL和TSV的S参数，以及pi型电路的S参数，得到整体结构的S参数。
4. 使用优化算法优化pi型电路的参数，使得级联网络的S参数与HFSS仿真得到的S参数尽可能接近。整体器件结构中共有12个连接处，共12个pi型电路，这里先对12个pi型电路使用相同的电路参数进行优化。
5. 使用优化效果较好的电路参数，作为初始值，对所有样本再次优化。
6. 导出优化得到的pi型电路参数以及对应器件参数，构建数据集。
7. 使用6中构建的数据集，训练神经网络，实现器件参数到pi型电路参数的映射。（神经网络架构详见Appendix 2）
8. 以7中训练好的神经网络为初步模型，再次以HFSS仿真得到的S参数为目标，使用优化算法优化神经网络的权重和偏置，使得神经网络输出的pi型电路参数构建的级联网络的S参数与HFSS仿真得到的S参数尽可能接近(训练时以S参数幅值和相位为目标)。这里设定每一连接处pi型网络电路参数不同(第一个pi型网络和最后一个相同，第二个与倒数第二个相同，以此类推，共6个不同的网络)，但是神经网络前几层的权重和偏置相同，最后设置多个输出头并训练得到不同权值和阈值。(多头神经网络架构详见Appendix 3)

Appendix 1: pi型电路结构
Y参数：
Y1 = j*w*Cn1 + 1/Rn1
Y2 = j*w*Cn2 + 1/Rn2
Y3 = j*w*Cn3 + 1/(Rn3 + j*w*Ln1)
ABCD矩阵：
A = 1 + Y2/Y3
B = 1/Y3
C = Y1 + Y2 + Y1*Y2/Y3
D = 1 + Y1/Y3

Appendix 2: DNN架构
输入参数 -> 30*30*20 -> 1
每个神经网络对应pi型电路中的一个元件值，共7个神经网络，分别对应Cn1、Cn2、Cn3、Rn1、Rn2、Rn3、Ln1

Appendix 3: 多头神经网络架构
输入参数 -> 30 -> 30 -> 20(*6) -> 1
其中20个节点的隐藏层有并列的6个输出头，每个输出头对应一个连接处的pi型电路参数。共12个连接处,其中第一个连接处和最后一个连接处的pi型电路参数相同，第二个与倒数第二个相同，以此类推。

Appendix 4: 器件参数(单位：um)
TMRDL:
pitch = 40~60
l_tmrdl = 100~700
w_tmrdl = 10~30
t_tmrdl = 2~5

BSMRDL:
pitch = 40~60
l_bsmrdl = 100~700
w_bsmrdl = 10~30
t_bsmrdl = 2~5

TSV:
h_tsv = 50~100
r_tsv = 5~15
pitch = 40~60

RDL_TSV:
h_tsv = 50~100
r_tsv = 5~15
pitch = 40~60
l_tmrdl = 100~700
w_tmrdl = 10~30
t_tmrdl = 2~5
l_bsmrdl = 100~700
w_bsmrdl = 10~30
t_bsmrdl = 2~5


Appendix 5: 误差计算方法
NMSE = sum((y_true - y_pred)^2) / sum((y_true - mean(y_true))^2)
其中y为S11、S21的实部和虚部


Appendix 6: HFSS仿真数据路径
1. 在SyncFiles\Temp\HFSS_sim\LHS400_Connection2 路径下，RDL和TSV数据建立RDL和TSV的等效电路模型。
2. 训练级联网络的神经网络时，使用C:\ffzhzh\SyncFiles\Temp\HFSS_sim\LHS150_50_Connection2 路径下train中的数据，使用test中的数据测试模型。




# v11 model （代码参考v10 model）
器件结构：TMRDL-TSV-BSMRDL-TSV-TMRDL-TSV-BSMRDL-TSV-TMRDL-TSV-BSMRDL-TSV-TMRDL 级联结构。器件参数见Appendix 4。
建模数据位置：详见Appendix 6。
建模方法：
1. 使用ADS仿真得到TMRDL、BSMRDL、TSV的S参数，级联得到整体结构的S参数。其中TMRDL、BSMRDL使用同一个ADS仿真设置，TSV使用一个ADS仿真设置。
2. 在每一个RDL和TSV的连接处，插入一个级联网络（pi型电路，详见Appendix 1）。
3. 使用优化算法优化pi型电路的参数，使得级联网络的S参数与ADS仿真得到的S参数尽可能接近。
4. 使用优化得到的pi型电路参数，和对应的器件参数构建数据集。用于训练神经网络，实现器件参数到pi型电路参数的映射。
5. 使用训练好的神经网络，输入器件参数，得到对应的pi型电路参数。然后利用这些参数构建级联网络，连接使用ADS仿真得到RDL和TSV的S参数，得到整体结构的S参数。

具体建模流程
1. 在.\HFSS_sim\LHS400_Connection2 目录下选取20组RDL和20组TSV数据校准ADS仿真设置，使得ADS仿真得到的RDL和TSV的S参数与HFSS仿真得到的S参数尽可能接近。
2. 读取HFSS仿真得到的S参数，使用对应的器件参数，使用ADS仿真得到RDL和TSV的S参数
3. 级联ADS仿真得到的RDL和TSV的S参数，以及pi型电路的S参数，得到整体结构的S参数。
4. 使用优化算法优化pi型电路的参数，使得级联网络的S参数与HFSS仿真得到的S参数尽可能接近。整体器件结构中共有12个连接处，共12个pi型电路，这里先对12个pi型电路使用相同的电路参数进行优化。
5. 导出优化得到的pi型电路参数以及对应器件参数，构建数据集。(排除优化不好的样本,MSE>0.1的样本)
6. 使用5中构建的数据集，训练神经网络，实现器件参数到pi型电路参数的映射。（神经网络架构详见Appendix 2）
7. 以6中训练好的神经网络为初步模型，再次以HFSS仿真得到的S参数为目标，使用优化算法优化神经网络的权重和偏置，使得神经网络输出的pi型电路参数构建的级联网络的S参数与HFSS仿真得到的S参数尽可能接近(训练时以S参数幅值和相位为目标)。这里设定每一连接处pi型网络电路参数不同，但是神经网络前几层的权重和偏置相同，最后设置多个输出头并训练得到不同权值和阈值。(多头神经网络架构详见Appendix 3)

Appendix 1: pi型电路结构
Y参数：
Y1 = j*w*Cn1 + 1/Rn1
Y2 = j*w*Cn2 + 1/Rn2
Y3 = j*w*Cn3 + 1/(Rn3 + j*w*Ln1)
ABCD矩阵：
A = 1 + Y2/Y3
B = 1/Y3
C = Y1 + Y2 + Y1*Y2/Y3
D = 1 + Y1/Y3

Appendix 2: DNN架构
输入参数 -> 30*30*20 -> 1
每个神经网络对应pi型电路中的一个元件值，共7个神经网络，分别对应Cn1、Cn2、Cn3、Rn1、Rn2、Rn3、Ln1

Appendix 3: 多头神经网络架构
输入参数 -> 30 -> 30 -> 20(*12) -> 1
其中20个节点的隐藏层有并列的12个输出头，每个输出头对应一个连接处的pi型电路参数，共12个连接处。

Appendix 4: 器件参数(单位：um)
TMRDL:
pitch = 40~60
l_tmrdl = 100~700
w_tmrdl = 10~30
t_tmrdl = 2~5

BSMRDL:
pitch = 40~60
l_bsmrdl = 100~700
w_bsmrdl = 10~30
t_bsmrdl = 2~5

TSV:
h_tsv = 50~100
r_tsv = 5~15
pitch = 40~60

RDL_TSV:
h_tsv = 50~100
r_tsv = 5~15
pitch = 40~60
l_tmrdl = 100~700
w_tmrdl = 10~30
t_tmrdl = 2~5
l_bsmrdl = 100~700
w_bsmrdl = 10~30
t_bsmrdl = 2~5


Appendix 5: 误差计算方法
NMSE = sum((y_true - y_pred)^2) / sum((y_true - mean(y_true))^2)
其中y为S11、S21的实部和虚部


Appendix 6: HFSS仿真数据路径
1. 在SyncFiles\Temp\HFSS_sim\LHS400_Connection2 路劲下，随机选取RDL和TSV各10~20个样本数据校准ADS仿真设置。
2. 训练级联网络的神经网络时，使用C:\ffzhzh\SyncFiles\Temp\HFSS_sim\LHS150_50_Connection2 路径下train中的数据，使用test中的数据测试模型。
