# v00 Parameter Extraction And Dataset Building

定位：模型训练前的数据准备和参数提取流程。

代码入口：
- `code/提参2.py`
- `code/提参3.py`
- `code/extract_rdl_top_params.py`
- `code/extract_tsv_params.py`

默认输出：
- `training_datasets/`

本目录不保存大体量结果；参数提取后的训练 CSV 统一放在根目录 `training_datasets/`。
